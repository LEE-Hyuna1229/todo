Question
> 정수 n이 주어질 때, n이하의 짝수를 모두 더한 값을 return 하도록 solution 함수를 작성해주세요.

Answer
1. 내가 푼 코드
> def solution(n):
    listnum = []
    for i in range(1,n+1):
        if (i%2) == 0:
            listnum.append(i)
    return sum(listnum)

2. 다른분 코드 참고해서 다시 푼 것
> def solution(n):
    return sum(range(0,n+1,2))

👩‍💻 깔끔한 코드 머싯다,,편하다,,
